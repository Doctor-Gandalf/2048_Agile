from model.tiles.tile import Tile
from model.tiles.numbertiles import *
__author__ = 'Kellan Childers'


def draw_tile(window, tile, dimensions, position):
    """Draw a tile at a place on the board

    :param window: The window to draw the tile to
    :param tile: The tile to draw
    :type tile: Tile
    :param dimensions: The size of the area to draw the tile in
    :param position: The position of the top-left corner of the tile
    :return:
    """
    pass
